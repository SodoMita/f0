STATUS — task board (digest of docs/STATUS.md; the .md is authoritative)

DONE (highlights): skeleton+router · relay pool + live feed · Blossom+SHA ·
local posters (auto-fit, frameDistance) · board (1-3 col, virtualised,
inertia, badges) · preview slot pool (viewport-gated, FPS cap) · viewer
(orbit + authored cameras, single-model guard) · thread map (pan/pinch/zoom,
tidy tree, per-node reply pills) · HTML HUD · demand-driven render loop
(kick(), render-on-demand, adaptive resolution) · comprehensive settings
(~50 settings, 11 groups, presets, src/settings/schema.ts) · deletion service
+ deletion UI · studio publish flow (BUD-01 + kind-1063 + ownership; merged) ·
first-run legend · network panel · error sheets · local Draco + GLB limits ·
standalone + GitHub Pages + mirrors (site branch / jsDelivr / githack) ·
search by name or content (topbar menu filters the board; own posts self-index at publish, restore from storage AND load from the seeded verified snapshot with zero network, so search always finds them and they always open; fresh posts never race their own upload; E101 retry really retries with per-replica detail; NIP-50 nostr.band fallback reaches unloaded remote models) ·
perf passes (workers, PBO, curated loader) · design passes + mirror/flip fixes ·
sidecar import branch merged and hardened (local-only, bounded, validate-first) ·
AES-GCM deletion-key envelopes · schema-validated persisted settings · data-URI
texture decoded-memory guards.

INCIDENT LOG: Pages had a 503 outage (artifact fine, re-run); main was once
force-pushed away and restored — unlanded work kept on salvage/dev-rewrite
(publish.ts, textMesh.ts, gui experiments); CHERRY-PICK from there, don't
rewrite. Branch protection now blocks force-pushes.

IN PROGRESS: paint editor (hand-writing ink) claimed 2026-08-18.

NEXT — EDITOR CORE FIRST (primary use cases):
 1 paint editor, primary = hand-writing text (ink: pressure, coalesced events,
   smoothing, eraser, undo; then selection + gizmos + thin instances)
 2 animation editor (timeline, keyframes, camera fly-through, glTF export)
 3 audio recording (getUserMedia + MediaRecorder, embed into GLB <=8MiB)
 4 phone-pose camera while recording (DeviceOrientation rotation; WebXR for
   position; sync pose+audio on one clock -> glTF camera anim)

REST: 3D thread view mode (AMENDMENT 39: 2D posters / tree / 3D models with
the main-camera-as-model-transform rule) · VR support (AMENDMENT 37, WebXR
viewer) · new settings entries (thread mode + VR toggle into the schema) ·
low-poly text geometry · tests (Vitest + Playwright).

DEBT: poster/preview pool shares one scene (mutex) · no service worker.
